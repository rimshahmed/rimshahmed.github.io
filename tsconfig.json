/**
 * A tiny shared flag. Set `attention.on = true` from anywhere and the
 * sentinel's eye flares brighter. Lives in its own module so that importing
 * it doesn't drag the whole Three.js bundle into the main chunk.
 */
export const attention = { on: false }
